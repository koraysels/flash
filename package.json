{
  "name": "flash",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "dev": "concurrently --names backend,frontend --prefix-colors blue,green \"pnpm --filter flash-backend dev\" \"pnpm --filter flash-frontend dev\"",
    "build": "pnpm --filter flash-backend build && pnpm --filter flash-frontend build"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "typescript": "^5.5.3"
  },
  "private": true,
  "pnpm": {
    "onlyBuiltDependencies": [
      "@prisma/client",
      "@prisma/engines",
      "canvas",
      "esbuild",
      "ffmpeg-static",
      "onnxruntime-node",
      "prisma",
      "yt-dlp-exec"
    ]
  }
}
